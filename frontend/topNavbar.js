import { View, Text} from "react-native"

function TopNavbar () {

return (

    <View style={{displayMode: "flex", alignItems: "center", backgroundColor: "white", shadowColor: "black"}}>
        <Text> Thrif</Text>
    </View>

)
}

export default TopNavbar;